#include <math.h>
#include <stdio.h>

int main() { int i,N;
             float x;
             float from,to;
             float inc; float diff; 
             float a[3]; float quadratic(float a[3], float x);
             float w;float sum=0;
           

             
from=0; to=10; N=20; 
a[0]=1,a[1]=1,a[2]=-10,a[3]=1;
             inc= (to-from)/N; x=from;

for (i=0; i<N; i++ ) {

f(x)dx= 
printf("%f ", x);

x+=inc;

}

return 0;
                  
}

             

{

return (fn (a,x+diff) - fn (a,x))/diff;
}


float quadratic(float a[3], float x){
	return a[0] + a[1]*x + a[2]*pow(x,2);
}


 

